{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "scripts"
  ],
  "scripts": {
    "dev": "npm run build --workspace=artifacts/dhd-livraison && npm run build --workspace=artifacts/api-server && PORT=3000 node artifacts/api-server/dist/index.mjs",
    "build": "npm run build --workspace=artifacts/dhd-livraison && npm run build --workspace=artifacts/api-server",
    "start": "PORT=3000 node artifacts/api-server/dist/index.mjs"
  },
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
