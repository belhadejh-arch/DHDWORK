{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "packageManager": "pnpm@10.26.1",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "scripts"
  ],
  "scripts": {
    "dev": "pnpm --filter @workspace/dhd-livraison run build && pnpm --filter @workspace/api-server run dev",
    "build": "pnpm --filter @workspace/dhd-livraison run build && pnpm --filter @workspace/api-server run build",
    "start": "node artifacts/api-server/dist/index.mjs"
  },
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
